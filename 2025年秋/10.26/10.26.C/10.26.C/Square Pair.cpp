#include <iostream>
#include <cmath>
#include <vector>
using namespace std;

int main()
{
	int n;
	cin >> n;

	//vector<long long> numbers(n);

	//for (int i = 0; i < n; i++)
	//{
	//	cin >> numbers[i];
	//}

	//int times=0;
	//for (int i = 0; i < n; i++)
	//{
	//	for (int j = i+1; j < n; j++)
	//	{
	//		long long root = sqrt(numbers[i] * numbers[j]);
	//		if (root*root== numbers[i] * numbers[j])
	//		{
	//			times += 1;
	//		}
	//	}
	//}

	//cout << times;

	return 0;;
}